var char= prompt("Enter a character");
if ( (char === 'a' || 'A' ) || (char === 'e' || 'E') || (char === 'i' || 'I' ) || (char === 'o' || 'O') || (char === 'u' || 'U')){

    alert("true");
}
else{
    alert("false");
}